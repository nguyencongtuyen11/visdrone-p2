# conf=0.05, min_slice_utility=0.0 (chấp nhận nhiều slice hơn -> tăng recall). Inference-only.
includes:
  - default.yaml
infer:
  output_conf: 0.05
  min_slice_utility: 0.0
  min_slice_detections: 1
